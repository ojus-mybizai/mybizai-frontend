/**
 * /team/templates — Task template CRUD (owner-only).
 *
 * Re-exports the legacy /tasks/templates page. Templates surface as a proper
 * Team sub-item now instead of being buried behind a small link on the member
 * pane.
 */
export { default } from '../../tasks/templates/page';
